node_modules/
dist/
.astro/
.env
.env.local
.vercel/
.netlify/

# Compiled CSS — regenerated by `npm run build:css`, never committed.
public/styles/app.css

# Promo video renders — hosted externally (Cloudflare R2 / GitHub releases).
# Keep poster.svg tracked as a lightweight fallback.
public/videos/*.mp4
public/videos/*.webm

